# Masjid Prayer Time Display — Spec Workflow

> **Stack:** React 18 + TypeScript + MUI v7 + Vite + Yarn  
> **Migration:** Remove Tailwind CSS & shadcn/ui → MUI only  
> **Package Manager:** Yarn (not npm, not pnpm)

---

## Table of Contents

- [Phase 0: Project Bootstrap & Cleanup](#phase-0-project-bootstrap--cleanup)
- [Phase 1: P0 — Critical Fixes](#phase-1-p0--critical-fixes)
- [Phase 2: P1 — High Priority](#phase-2-p1--high-priority)
- [Phase 3: P2 — Medium Priority](#phase-3-p2--medium-priority)
- [Phase 4: P3 — Low Priority](#phase-4-p3--low-priority)
- [Phase 5: Final Validation](#phase-5-final-validation)

---

## Phase 0: Project Bootstrap & Cleanup

### Step 0.1 — Switch to Yarn & Lockfile

> **Prompt:**
> ```
> The project currently uses pnpm (pnpm-workspace.yaml exists). Remove all pnpm
> artifacts. Delete pnpm-lock.yaml if present, delete pnpm-workspace.yaml, and
> delete node_modules. Then run `yarn install` to generate a yarn.lock. Ensure
> the package.json scripts work with yarn (dev, build). Update README.md to say
> `yarn install` and `yarn dev` instead of npm.
> ```

### Step 0.2 — Remove Tailwind CSS Infrastructure

> **Prompt:**
> ```
> Remove Tailwind CSS v4 and its Vite plugin from the project entirely:
> 1. Uninstall: @tailwindcss/vite, tailwindcss, tw-animate-css, tailwind-merge
> 2. Delete src/styles/tailwind.css
> 3. Remove the tailwindcss() import from vite.config.ts
> 4. Remove postcss.config.mjs (no longer needed)
> 5. Remove any @import 'tailwindcss' or @tailwind directives from any CSS file
> 6. Remove all Tailwind utility classes from all components (we will replace
>    them with MUI in later steps, for now just strip them — components will
>    look broken temporarily and that's OK)
> 7. Keep the @theme inline block in theme.css as CSS custom properties only
>    (they are useful as design tokens), but remove any Tailwind-specific
>    syntax like @apply, @source, source(none)
> ```

### Step 0.3 — Remove shadcn/ui Components

> **Prompt:**
> ```
> Delete the entire src/app/components/ui/ directory (all 48 shadcn component
> files). None of them are used by the app — they were auto-generated by the
> Figma export. Also delete src/app/components/figma/ImageWithFallback.tsx as
> it's unused. Also remove these shadcn-related dependencies from package.json:
> - All @radix-ui/* packages
> - class-variance-authority
> - cmdk
> - input-otp
> - vaul
> - sonner
> - react-resizable-panels
> - react-day-picker
> - embla-carousel-react
> Run yarn install after updating package.json.
> ```

### Step 0.4 — Remove Unused Non-MUI Dependencies

> **Prompt:**
> ```
> Remove these unused dependencies from package.json:
> - recharts (no charts used)
> - react-dnd and react-dnd-html5-backend (no drag and drop)
> - react-slick and slick-carousel (using custom carousel)
> - react-hook-form (no forms)
> - canvas-confetti (no confetti)
> - react-popper and @popperjs/core (not used directly)
> - react-responsive-masonry (not used)
> - react-router (no routing, single page)
> - next-themes (not used, dark mode is via CSS class)
> - clsx (will use MUI's sx prop or utility)
>
> Keep these — they ARE used:
> - @mui/material and @mui/icons-material (core stack)
> - @emotion/react and @emotion/styled (MUI peer deps)
> - motion (Framer Motion animations)
> - lucide-react (icons used across components)
> - date-fns (will need for date calculations)
> - react-dom and react (core)
>
> Run yarn install after cleanup. Verify the app still builds with yarn build.
> ```

### Step 0.5 — Remove Dead Files & Duplicates

> **Prompt:**
> ```
> Delete these dead/unused files:
> - src/app/components/InfoPanels.tsx (unused, superseded by HadithPanel + WeatherWidget)
> - src/styles/globals.css (empty file, never imported)
> - default_shadcn_theme.css (duplicate of src/styles/theme.css)
> - src/styles/fonts.css contents should be kept but we'll switch to MUI's
>   font loading via CssBaseline in the theme setup step
> ```

### Step 0.6 — Set Up MUI Theme & Provider

> **Prompt:**
> ```
> Create a proper MUI v7 theme configuration for this Masjid Prayer Time Display app.
> The app uses a dark emerald/gold Islamic aesthetic:
>
> 1. Create src/app/theme/muiTheme.ts with:
>    - Dark palette: background #0a1f0a, paper/card: rgba(0,0,0,0.3)
>    - Primary color: #D4AF37 (gold)
>    - Secondary color: #FFD700 (lighter gold)
>    - Text: white primary, gray-400 secondary
>    - Error: #d4183d (from existing theme)
>    - Font family: 'Open Sans' for English, 'Noto Naskh Arabic' for Arabic
>    - Components overrides for Button, Card, Paper, Typography with the gold
>      border aesthetic and backdrop-blur glass effect
>    - RTL support configuration (direction awareness)
>
> 2. Create src/app/theme/ThemeProviderWrapper.tsx that wraps children with:
>    - MUI ThemeProvider
>    - CssBaseline (with dark mode, font injection for Open Sans + Noto Naskh Arabic)
>    - Emotion CacheProvider
>
> 3. Update src/main.tsx to wrap <App /> with the ThemeProviderWrapper
>
> 4. Import the Google Fonts via CssBaseline or a <link> in index.html (replace
>    the current fonts.css @import approach)
>
> All existing components are still stripped of styles — this just sets up the
> MUI foundation.
> ```

### Step 0.7 — Git Checkpoint

> **Prompt:**
> ```
> Stage all changes and commit with message:
> "refactor: bootstrap MUI stack, remove Tailwind/shadcn/unused deps"
> ```

---

## Phase 1: P0 — Critical Fixes

### Step 1.1 — Create Shared Utilities Module

> **Prompt:**
> ```
> Create src/app/utils/helpers.ts with these shared utilities extracted from
> duplicated code across components:
>
> 1. toArabicNumerals(text: string): string — converts Western digits to Arabic
>    numerals. Currently duplicated in PrayerCard, CountdownBar, WeatherWidget,
>    FundraisingOverlay, InfoPanels.
>
> 2. getFontFamily(language: Language): string — returns 'Noto Naskh Arabic, serif'
>    for 'ar' and 'Open Sans, sans-serif' for 'en'. Currently duplicated in 7 files.
>
> 3. isRTL(language: Language): boolean — returns language === 'ar'.
>
> 4. getDirection(language: Language): 'rtl' | 'ltr' — returns the dir attribute value.
>
> 5. getPrayerTimesFromAPI(): Promise<PrayerTimes> — placeholder async function
>    that will eventually call the Aladhan API. For now return hardcoded data
>    but with the correct TypeScript interface.
>
> Export the Language type from translations.ts and re-export from this file.
> Update all 7+ components to import from this shared module instead of
> duplicating. Remove the duplicated inline versions.
> ```

### Step 1.2 — Type the Translations Object

> **Prompt:**
> ```
> In src/app/utils/translations.ts:
>
> 1. Create a proper TypeScript interface called `Translations` (or use
>    `satisfies typeof translations.en`) that describes the full shape of the
>    translation object — including nested objects like `prayers`, `event`,
>    `fundraising`, `weather`, `days`, `months`, and the arrays like
>    `announcementsList`.
>
> 2. Type the export as: `export const translations: Record<Language, Translations>`
>
> 3. Update every component that currently accepts `translations: any` to accept
>    `translations: Translations` instead. Components affected:
>    - Header, MasjidInfo, HadithPanel, WeatherWidget, AnnouncementsTicker,
>      FundraisingOverlay, EventModeDisplay, App
>
> Import Translations from the translations module in each file.
> ```

### Step 1.3 — Dynamic Prayer Time Calculation

> **Prompt:**
> ```
> Replace the hardcoded prayer time system with a dynamic one:
>
> 1. Create src/app/utils/prayerTimes.ts with:
>    - A PrayerTime interface: { name: string; key: string; time: string;
>      iqamaTime: string; minutesFromMidnight: number }
>    - A function getCurrentPrayer(prayers: PrayerTime[]): PrayerTime — determines
>      which prayer is currently active based on current time
>    - A function getNextPrayer(prayers: PrayerTime[]): PrayerTime — determines
>      the next upcoming prayer
>    - A function getTimeToNextPrayer(nextPrayer: PrayerTime): { hours: number;
>      minutes: number; seconds: number } — computes countdown
>
> 2. In App.tsx:
>    - Replace `const currentPrayer = "Dhuhr"` with a useState that derives from
>      getCurrentPrayer()
>    - Replace the hardcoded nextPrayer/nextPrayerTime passed to CountdownBar
>      with dynamic values from getNextPrayer()
>    - Remove the hardcoded `prayerTimesInMinutes` array and derive from prayer data
>    - Update prayers array to use computed currentPrayer and nextPrayer
>
> 3. For now, prayer times can still be hardcoded values (05:30, 12:45, etc.)
>    but the CURRENT and NEXT prayer detection must be dynamic based on actual
>    clock time.
>
> 4. The isNearPrayerTime() function should use the shared prayer data, not its
>    own separate array.
> ```

### Step 1.4 — MUI Migration: Header Component

> **Prompt:**
> ```
> Rewrite src/app/components/Header.tsx to use MUI v7 instead of Tailwind CSS
> utility classes. Requirements:
>
> - Use MUI AppBar (position="static", dark background with gold border bottom)
> - Use MUI Toolbar for layout
> - Use MUI Button for language toggle, donate button, event mode toggle
>   - Language button: variant="outlined", sx with gold border, gold text
>   - Donate button: Heart icon from @mui/icons-material (replace lucide Heart),
>     outlined variant
>   - Event mode button: variant="contained", gold background, black text
> - Use MUI Typography for the clock display (font-mono, tracking-wider)
> - Keep lucide-react icons for Languages, CalendarClock (or switch to MUI icons)
> - Keep the setInterval clock logic
> - Make it responsive using MUI's sx breakpoints (sm, md, lg)
> - Keep the language toggle logic and event mode toggle logic
> - Properly type props using the Translations interface (not `any`)
> ```

### Step 1.5 — MUI Migration: PrayerCard Component

> **Prompt:**
> ```
> Rewrite src/app/components/PrayerCard.tsx to use MUI v7:
>
> - Use MUI Card with dark semi-transparent background (rgba(0,0,0,0.3))
> - Use MUI CardContent for inner content
> - Active card: gold border (borderColor: '#D4AF37'), box-shadow glow
>   (0 0 30px rgba(212,175,55,0.5)), slightly scaled up via sx transform
> - Inactive card: subtle gold border (rgba(212,175,55,0.3))
> - Use MUI Typography for prayer name, time, iqama text
>   - Prayer name: gray-400, uppercase, tracking-wider, caption variant
>   - Prayer time: white, bold, large (h4 for active, h5 for inactive)
>   - Iqama: gold text, small
> - Use lucide-react icons (Sunrise, Sun, CloudSun, Sunset, Moon, Star)
>   for each prayer, colored gold when active, gray when inactive
> - Import toArabicNumerals and getFontFamily from shared utils
> - Use the PrayerCardProps interface with proper Translations type
> - Support RTL via dir attribute on the Card
> ```

### Step 1.6 — MUI Migration: CountdownBar Component

> **Prompt:**
> ```
> Rewrite src/app/components/CountdownBar.tsx to use MUI v7:
>
> - Use MUI Paper with dark background, gold border, rounded corners
> - Use MUI Box with flexbox for layout (justify-between)
> - Use MUI Typography for:
>   - Prayer label text ("Next prayer in" / Arabic equivalent)
>   - Countdown timer (gold, font-mono, bold)
> - Keep the countdown calculation logic using useEffect/setInterval
> - Import shared utilities (toArabicNumerals, getFontFamily, getDirection)
> - Support RTL
> - Use proper Translations type
> ```

### Step 1.7 — MUI Migration: MasjidInfo Component

> **Prompt:**
> ```
> Rewrite src/app/components/MasjidInfo.tsx to use MUI v7:
>
> - Use MUI Box for layout (flex row, align center, justify between)
> - Use MUI Avatar or just an img for the masjid logo (responsive sizing)
> - Use MUI Typography for:
>   - Hijri date (white text)
>   - Gregorian date (gray-400 text)
>   - Separator dot
> - Remove the unused logoSvg import (only use image_logo_masjid_design_1)
> - Keep the setInterval for live Gregorian date
> - Keep getHijriDate() as placeholder (will be replaced with real calculation)
> - Import shared utilities
> - Support RTL
> ```

### Step 1.8 — MUI Migration: HadithPanel Component

> **Prompt:**
> ```
> Rewrite src/app/components/HadithPanel.tsx to use MUI v7:
>
> - Use MUI Paper with dark bg, gold border, rounded
> - Use MUI Typography for:
>   - Title "Hadith of the Day" — gold, uppercase, tracking-wider, bold
>   - Hadith text — white, italic, centered, larger text
>   - Source — gray-400, smaller, right-aligned (left for RTL)
> - Use MUI Box for layout and spacing
> - Import shared utilities
> - Support RTL
> ```

### Step 1.9 — MUI Migration: WeatherWidget Component

> **Prompt:**
> ```
> Rewrite src/app/components/WeatherWidget.tsx to use MUI v7:
>
> - Use MUI Paper with dark bg, gold border
> - Use MUI Box for flex layout
> - Use @mui/icons-material Cloud icon (replacing lucide Cloud)
> - Use @mui/icons-material WaterDrop or Droplets for humidity
> - Use MUI Typography for:
>   - Temperature (white, bold, large)
>   - City name (gray-400)
>   - Weather condition text (gray-300)
>   - Humidity percentage (gray-400)
> - For now keep hardcoded temperature/humidity values — API integration later
> - Import shared utilities (toArabicNumerals, getFontFamily)
> - Support RTL
> ```

### Step 1.10 — MUI Migration: AnnouncementsTicker

> **Prompt:**
> ```
> Rewrite src/app/components/AnnouncementsTicker.tsx to use MUI v7:
>
> - Use MUI AppBar (position="fixed" bottom or static) or Paper for the ticker bar
> - Use MUI Box for the scrollable area
> - Use MUI Typography for announcement text
> - Use @mui/icons-material Campaign or Notifications for the megaphone icon
> - Keep the requestAnimationFrame scroll animation logic
> - Use MUI Avatar or img for the logo on the left side
> - The gold "announcements" badge on the right — use MUI Chip or styled Box
> - Support RTL (reverse scroll direction)
> - Import shared utilities
> ```

### Step 1.11 — MUI Migration: FundraisingOverlay

> **Prompt:**
> ```
> Rewrite src/app/components/FundraisingOverlay.tsx to use MUI v7:
>
> - Use MUI Dialog or Backdrop for the overlay (fixed, full screen, blur bg)
> - Use MUI Paper for the card (dark bg, gold border, rounded)
> - Use MUI IconButton for the close button (X icon)
> - Use MUI Typography for all text (title, description, labels)
> - Use MUI LinearProgress for the fundraising progress bar
>   - Gold gradient from #D4AF37 to #FFD700
> - Use MUI Box for layout grids (collected | goal | donors section)
> - Use MUI Divider (vertical) between the stat columns
> - Keep the auto-close countdown logic (10 seconds)
> - Import shared utilities (toArabicNumerals, getFontFamily)
> - Support RTL — close button should flip to top-left in RTL
> ```

### Step 1.12 — MUI Migration: EventModeDisplay

> **Prompt:**
> ```
> Rewrite src/app/components/EventModeDisplay.tsx to use MUI v7:
>
> - Use MUI Box for all layout (absolute positioning, flex centering)
> - Use MUI Paper for the event card (dark bg, gold border, rounded)
> - Use MUI Chip for the event type badge ("Special Islamic Lecture")
> - Use MUI Typography for event title, speaker name, description
> - Use MUI Grid2 for the 3-column details grid (date, time, location)
> - Use @mui/icons-material: CalendarMonth, AccessTime, LocationOn, Groups
> - Use MUI Button (contained, gold gradient) for the CTA
> - Keep all Framer Motion animations (motion.div wrappers)
> - Decorative corner ornaments — use MUI Box with sx borders
> - Import shared utilities
> - Support RTL
> ```

### Step 1.13 — MUI Migration: IslamicGeometricOverlay

> **Prompt:**
> ```
> Rewrite src/app/components/IslamicGeometricOverlay.tsx to use MUI v7:
>
> - Use MUI Box for all containers (absolute, inset-0, pointer-events-none)
> - Keep the SVG patterns and Framer Motion animations as-is (they're pure SVG)
> - Replace Tailwind classes with MUI sx prop for:
>   - Absolute positioning (position: 'absolute', inset: 0)
>   - Opacity values
>   - The gold accent glow bars (use sx background gradient)
>   - Corner accent lights (use sx with radial-gradient)
> - Fix the Math.random() issue: move particle positions to useMemo or
>   constant array so they're deterministic across renders
> - Reduce particle count from 12 to 6 for performance (kiosk display)
> ```

### Step 1.14 — MUI Migration: ImageCarousel

> **Prompt:**
> ```
> Rewrite src/app/components/ImageCarousel.tsx to use MUI v7:
>
> - Use MUI Box for the carousel container
> - Use MUI IconButton for prev/next navigation buttons
> - Use @mui/icons-material ChevronLeft, ChevronRight
> - Use MUI Box for dot indicators (styled as small circles)
> - Keep the auto-slide interval logic
> - Keep AnimatePresence for image transitions
> - Use sx prop for all styling (rounded, border, overflow hidden)
> - Add proper aria-labels on navigation buttons (keep existing a11y)
> ```

### Step 1.15 — MUI Migration: App.tsx Root

> **Prompt:**
> ```
> Rewrite src/app/App.tsx to use MUI v7:
>
> - Replace all Tailwind classes with MUI sx prop or styled components
> - Use MUI Box, Stack for layout
> - The diagonal grid background pattern — use sx backgroundImage on a Box
> - Keep the AnimatePresence mode switching logic
> - Keep the fundraising timer logic (but fix the recursive setTimeout issue
>   — use a single useEffect with a ref to track scheduling)
> - Keep the language toggle and event mode toggle
> - Pass proper Translations types to all child components
> - Prayer cards grid: use MUI Grid2 with responsive columns
>   (2 cols mobile, 3 cols sm, 6 cols lg)
> - Fix the justify-content-center CSS (non-standard) — use MUI justifyContent
> ```

### Step 1.16 — Fix Fundraising Timer Leak

> **Prompt:**
> ```
> Fix the fundraising timer in App.tsx. The current implementation recursively
> calls setTimeout but only tracks the last reference. This means:
> 1. The cleanup function only clears one of potentially many pending timers
> 2. If the component re-renders, multiple chains can stack
>
> Fix using useRef to track the timer ID, and ensure the cleanup function
> properly clears it. Consider using a single useEffect with a calculated
> delay instead of recursive setTimeout. Add a flag to prevent multiple
> concurrent schedules.
> ```

### Step 1.17 — Git Checkpoint

> **Prompt:**
> ```
> Stage all changes and commit with message:
> "feat: complete MUI migration, dynamic prayer detection, shared utils"
> ```

---

## Phase 2: P1 — High Priority

### Step 2.1 — Shared Clock Hook (Single Timer)

> **Prompt:**
> ```
> Create src/app/hooks/useClock.ts:
>
> export function useClock(intervalMs: number = 1000): Date
>
> This hook creates a single setInterval that updates a state with new Date()
> at the given interval. Used by Header (clock display) and MasjidInfo (date).
>
> Then create src/app/hooks/useCountdown.ts:
>
> export function useCountdown(targetTime: string): {
>   hours: number; minutes: number; seconds: number; display: string;
> }
>
> Takes a time string like "16:15" and returns the countdown. Used by
> CountdownBar.
>
> Update Header, MasjidInfo, and CountdownBar to use these hooks instead of
> their own independent setInterval calls. This reduces from 3 re-renders/sec
> to 1 centralized clock.
> ```

### Step 2.2 — Proper Prayer Time Data Source (API Placeholder)

> **Prompt:**
> ```
> Create src/app/services/prayerTimesApi.ts:
>
> 1. Define interfaces:
>    - PrayerTimesResponse: the shape of data from Aladhan API
>    - PrayerTimesConfig: { latitude, longitude, method, date }
>
> 2. Create a service function:
>    async function fetchPrayerTimes(config: PrayerTimesConfig):
>      Promise<PrayerTime[]>
>
>    For now, this returns the hardcoded times wrapped in a Promise
>    (simulating an API call). Add a TODO comment for real API integration.
>    The real implementation should use the Aladhan API:
>    https://api.aladhan.com/v1/timings/{date}?latitude={lat}&longitude={lng}&method={method}
>
> 3. Create a React context PrayerTimesContext that:
>    - Fetches prayer times on mount (or at midnight for the new day)
>    - Provides prayer times, current prayer, next prayer to all components
>    - Handles loading and error states
>    - Re-fetches at midnight for the next day's times
>
> 4. Create src/app/hooks/usePrayerTimes.ts that consumes this context
>
> Update App.tsx to use the context provider and consume from it
> ```

### Step 2.3 — Weather API Placeholder

> **Prompt:**
> ```
> Create src/app/services/weatherApi.ts:
>
> 1. Define interfaces:
>    - WeatherData: { temperature: number; humidity: number; condition:
>      string; city: string; icon?: string }
>    - WeatherConfig: { city: string; units?: 'metric' | 'imperial' }
>
> 2. Create a service function:
>    async function fetchWeather(config: WeatherConfig):
>      Promise<WeatherData>
>
>    Returns hardcoded data for now (28°C, 75%, "Partly Cloudy", "Hanoi").
>    Add TODO for real weather API integration (e.g., OpenWeatherMap).
>
> 3. Update WeatherWidget to receive WeatherData as a prop instead of
>    hardcoding values
>
> 4. In App.tsx or a context, fetch weather on mount and refresh every 30 min
> ```

### Step 2.4 — Git Checkpoint

> **Prompt:**
> ```
> Stage all changes and commit with message:
> "feat: shared hooks, API service layer, context providers"
> ```

---

## Phase 3: P2 — Medium Priority

### Step 3.1 — Error Boundaries

> **Prompt:**
> ```
> Create src/app/components/ErrorBoundary.tsx:
>
> A React error boundary class component that:
> 1. Catches any JavaScript errors in child components
> 2. Displays a graceful fallback UI (MUI Typography message, "Something went
>    wrong. The display will recover automatically.")
> 3. Automatically attempts recovery after 5 seconds by resetting error state
> 4. Logs errors to console (or a future error reporting service)
>
> Wrap the main App content with this error boundary in main.tsx.
> For a kiosk display, auto-recovery is critical — the app should never show
> a white screen of death.
> ```

### Step 3.2 — Bundle Local Image Assets

> **Prompt:**
> ```
> Replace the hardcoded Unsplash URLs in App.tsx with local bundled images:
>
> 1. Download the 3 Unsplash images and save them to src/imports/ as:
>    - mosque-1.jpg
>    - mosque-2.jpg
>    - mosque-3.jpg
>
> 2. Import them in App.tsx as module assets (Vite handles this)
>
> 3. Update the carouselImages array to use local imports
>
> This ensures the carousel works offline — critical for a kiosk display.
> ```

### Step 3.3 — Offline Resilience (Optional)

> **Prompt:**
> ```
> Add basic offline resilience for the kiosk display:
>
> 1. Create a simple service worker at public/sw.js that:
>    - Caches the app shell (HTML, JS, CSS, fonts, images)
>    - Uses cache-first strategy for assets
>    - Uses network-first for API calls (prayer times, weather)
>    - Falls back to cached data if offline
>
> 2. Register the service worker in src/main.tsx
>
> 3. Add a "last updated" indicator somewhere subtle in the UI so the masjid
>    operator knows if data is stale
>
> Note: This step is optional and can be deferred. The app works without it
> but may show broken content if the network drops.
> ```

### Step 3.4 — Accessibility Improvements (Optional)

> **Prompt:**
> ```
> Improve accessibility for the prayer time display:
>
> 1. Add aria-live="polite" to the countdown timer region so screen readers
>    announce time changes (but not every second — perhaps every minute)
> 2. Add aria-live="polite" to the announcements ticker
> 3. Add proper aria-labels to all icon-only buttons
> 4. Add a `prefers-reduced-motion` media query check:
>    - If the user prefers reduced motion, disable the Islamic geometric overlay
>      rotation and particle animations
>    - Reduce the ticker scroll speed
>    - Disable the prayer card scale animations
>    Use MUI's useMediaQuery('(prefers-reduced-motion: reduce)') for this
> 5. Add keyboard focus management for the fundraising overlay (trap focus
>    when open, restore focus when closed)
>
> Note: For a kiosk display, a11y is lower priority but still good practice
> for screen reader users who may visit the masjid.
> ```

### Step 3.5 — Git Checkpoint

> **Prompt:**
> ```
> Stage all changes and commit with message:
> "feat: error boundary, local assets, a11y improvements"
> ```

---

## Phase 4: P3 — Low Priority

### Step 4.1 — Theme Token System

> **Prompt:**
> ```
> Replace all hardcoded color strings (#D4AF37, #0a1f0a, #FFD700, etc.) with
> MUI theme tokens and CSS custom properties:
>
> 1. In the MUI theme (src/app/theme/muiTheme.ts), define:
>    - theme.palette.gold.main = '#D4AF37'
>    - theme.palette.gold.light = '#FFD700'
>    - theme.palette.gold.dark = '#B8960F'
>    - theme.palette.emerald.main = '#0a1f0a'
>    - etc.
>
> 2. Create a src/app/theme/tokens.ts that exports constants for use in sx props:
>    export const GOLD = '#D4AF37';
>    export const GOLD_LIGHT = '#FFD700';
>    etc.
>
> 3. Update ALL components to use theme.palette.gold or the token constants
>    instead of hardcoded hex strings. Search for '#D4AF37' across all files.
>
> 4. This makes future theme changes (e.g., switching to a blue theme for
>    another masjid) a single-point change.
> ```

### Step 4.2 — Fix Conflicting Responsive Sizing in PrayerCard

> **Prompt:**
> ```
> In PrayerCard.tsx, there are conflicting responsive classes:
> The time Typography had both named Tailwind sizes (text-xl lg:text-2xl) and
> arbitrary values (text-[24px] sm:text-[48px]). Now that we're on MUI, ensure
> the Typography variant and sx prop use a single consistent responsive system:
>
> - Active card time: sx={{ fontSize: { xs: 24, sm: 48 } }} fontWeight: 'bold'
> - Inactive card time: sx={{ fontSize: { xs: 18, sm: 24, lg: 32 } }}
>
> Remove any remaining conflicting size definitions. Use ONLY MUI breakpoint
> objects in sx for responsive sizing.
> ```

### Step 4.3 — Remove Remaining Dead Code

> **Prompt:**
> ```
> Final cleanup pass:
>
> 1. Delete src/styles/fonts.css (fonts are now loaded via MUI CssBaseline)
> 2. Delete src/styles/index.css if it only imports deleted files
> 3. Delete src/styles/tailwind.css (already deleted in Phase 0, verify)
> 4. Remove any unused imports across all files
> 5. Remove the Languages import from App.tsx if still present (it's only
>    used in Header)
> 6. Ensure no .css files reference Tailwind or @apply
> 7. Run `yarn build` and verify zero errors
> 8. Run TypeScript strict mode check if possible
> ```

### Step 4.4 — Responsive Design Audit (Optional)

> **Prompt:**
> ```
> Do a responsive design audit of the full application using MUI breakpoints:
>
> 1. Test at xs (0-599px), sm (600-959px), md (960-1279px), lg (1280+)
> 2. Verify:
>    - Header: clock visible at all sizes, button text hides on mobile (icon-only)
>    - Prayer cards: 2 cols mobile, 3 cols sm, 6 cols lg
>    - CountdownBar + WeatherWidget: stack vertically on mobile, side-by-side lg
>    - Image carousel: appropriate height at each breakpoint
>    - Hadith panel: readable text size at all sizes
>    - Fundraising overlay: scrollable on small screens
>    - Event mode: text doesn't overflow
> 3. Fix any responsive issues found using MUI sx breakpoints
> ```

### Step 4.5 — Git Checkpoint

> **Prompt:**
> ```
> Stage all changes and commit with message:
> "refactor: theme tokens, responsive audit, dead code removal"
> ```

---

## Phase 5: Final Validation

### Step 5.1 — Full Build & Type Check

> **Prompt:**
> ```
> Run the following validation commands and ensure all pass:
>
> 1. yarn build — should complete with zero errors
> 2. yarn tsc --noEmit — TypeScript strict check, zero errors
> 3. Verify no console errors in the browser when running yarn dev
> 4. Verify the app looks correct in both English and Arabic modes
> 5. Verify event mode toggle works
> 6. Verify fundraising overlay appears and auto-closes
> 7. Verify countdown timer is ticking
> 8. Verify announcements ticker is scrolling
> 9. Verify image carousel is cycling
>
> Fix any issues found.
> ```

### Step 5.2 — Final Bundle Analysis

> **Prompt:**
> ```
> Analyze the production bundle size:
>
> 1. Install rollup-plugin-visualizer (or use vite-bundle-visualizer)
> 2. Build with bundle analysis enabled
> 3. Verify that MUI is the primary dependency (no Tailwind/shadcn remnants)
> 4. Check that unused MUI icons aren't bloating the bundle (use named imports
>    only: import CalendarMonth from '@mui/icons-material/CalendarMonth')
> 5. Target: total bundle < 500KB gzipped
>
> Report the final bundle size and composition.
> ```

### Step 5.3 — Final Commit & Tag

> **Prompt:**
> ```
> Stage all changes and commit with message:
> "chore: final validation, bundle optimization"
>
> Then tag the commit: git tag v1.0.0 -m "MUI migration complete"
> ```

---

## Appendix: File Tree After Migration

```
src/
├── main.tsx
├── imports/
│   ├── logo-masjid-design-1.png
│   ├── logo.png
│   ├── splash.svg
│   ├── mosque-1.jpg
│   ├── mosque-2.jpg
│   └── mosque-3.jpg
├── app/
│   ├── App.tsx
│   ├── theme/
│   │   ├── muiTheme.ts
│   │   ├── ThemeProviderWrapper.tsx
│   │   └── tokens.ts
│   ├── hooks/
│   │   ├── useClock.ts
│   │   ├── useCountdown.ts
│   │   └── usePrayerTimes.ts
│   ├── services/
│   │   ├── prayerTimesApi.ts
│   │   └── weatherApi.ts
│   ├── contexts/
│   │   └── PrayerTimesContext.tsx
│   ├── components/
│   │   ├── ErrorBoundary.tsx
│   │   ├── Header.tsx
│   │   ├── MasjidInfo.tsx
│   │   ├── PrayerCard.tsx
│   │   ├── CountdownBar.tsx
│   │   ├── HadithPanel.tsx
│   │   ├── WeatherWidget.tsx
│   │   ├── AnnouncementsTicker.tsx
│   │   ├── FundraisingOverlay.tsx
│   │   ├── IslamicGeometricOverlay.tsx
│   │   ├── EventModeDisplay.tsx
│   │   └── ImageCarousel.tsx
│   └── utils/
│       ├── translations.ts
│       ├── helpers.ts
│       └── prayerTimes.ts
├── styles/
│   └── theme.css (CSS custom properties only, no Tailwind)
index.html
vite.config.ts
package.json
yarn.lock
```

---

## Dependency List After Migration

### Production Dependencies
| Package | Purpose |
|---------|---------|
| react | Core |
| react-dom | Core |
| @mui/material | UI components |
| @mui/icons-material | Icons |
| @emotion/react | MUI styling engine |
| @emotion/styled | MUI styled API |
| motion | Animations (Framer Motion) |
| lucide-react | Supplementary icons |
| date-fns | Date calculations |

### Dev Dependencies
| Package | Purpose |
|---------|---------|
| vite | Build tool |
| @vitejs/plugin-react | JSX support |

**Total: ~9 prod + 2 dev deps** (down from 66 prod + 4 dev)
